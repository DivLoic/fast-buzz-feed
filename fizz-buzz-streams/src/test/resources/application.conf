application.id = LOCAL-FBZZ-APP

bootstrap.servers = "unused:9092"

input.topic.name = fizz-buzz-input
output.topic.name = fizz-buzz-output

schema.registry.url = "http://unused:8081"

window.step = 20 seconds
window.size = 20 seconds

confluent-cloud-client = {}
schema-registry-client = {}