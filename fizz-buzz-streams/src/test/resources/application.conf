application.id = TEST-BUZZ-APP

bootstrap.servers = "notused:1234"

input.topic.name = fizz-buzz-input
output.topic.name = fizz-buzz-output

schema.registry.url = "http://notused:8081"

window.step = 20 seconds
window.size = 20 seconds

topic {
  input.name = fizz-buzz-input
  input.name = ${?INPUT_TOPIC_NAME}

  output.name = fizz-buzz-output
  output.name = ${?OUTPUT_TOPIC_NAME}
}

kafka-client-connection = {}

schema-client-connection = {}